meta {
  name: Health Check
  type: http
  seq: 2
  tags: [
    health
    smoke
  ]
}

get {
  url: {{base_url}}/
  body: none
  auth: none
}

tests {
  test("returns 200", function() {
    expect(res.status).to.equal(200);
  });
}
