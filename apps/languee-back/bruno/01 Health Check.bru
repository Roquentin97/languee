meta {
  name: Health Check
  type: http
  seq: 1
  tags: [
    health
    smoke
  ]
}

get {
  url: {{base_url}}
}

tests {
  test("returns 200", function() {
    expect(res.status).to.equal(200);
  });
}
