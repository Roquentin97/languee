meta {
  name: Delete Meaning Link
  type: http
  seq: 4
  tags: [
    synonyms
  ]
}

delete {
  url: {{base_url}}/api/v1/synonyms/{{meaning_link_id}}
  body: none
  auth: inherit
}

vars:pre-request {
  meaning_link_id: 019ece7b-e9b5-78a5-a78a-75753bf93461
}

tests {
  test("deletes or reports not found", function() {
    expect(res.status).to.be.oneOf([204, 404]);
  });
}
