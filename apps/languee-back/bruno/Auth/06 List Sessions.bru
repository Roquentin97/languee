meta {
  name: List Sessions
  type: http
  seq: 2
  tags: [
    auth
  ]
}

get {
  url: {{base_url}}/auth/sessions
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{accessToken}}
}

vars:pre-request {
  jack_access_token: 
}

tests {
  test("returns active sessions", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.be.an("array");
  });
}
