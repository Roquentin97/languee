meta {
  name: List Cards
  type: http
  seq: 2
  tags: [
    cards
  ]
}

get {
  url: {{base_url}}/api/v1/cards
  body: none
  auth: inherit
}

params:query {
  deckId: {{deck_id}}
  ankiDroidExportStatus:
}

tests {
  test("returns cards array", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.be.an("array");
  });
}
