meta {
  name: Health Check
  type: http
  seq: 1
  tags: [
    health
    smoke
  ]
}

get {
  url: {{base_url}}/health
  body: none
  auth: none
}

vars:pre-request {
  base_url: 
}

tests {
  test("returns 200", function() {
    expect(res.status).to.equal(200);
    expect(res.body.status).to.equal("ok");
  });
}
